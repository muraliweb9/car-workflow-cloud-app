spring:
  boot:
    admin:
      client: # don't register on spring boot admin
        url:
    test:
      process:
        timeout: 20
eureka:
  client:
    registerWithEureka: false # don't register with Eureka
    fetchRegistry: false